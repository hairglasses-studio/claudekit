# Legacy Aliases

The canonical skill is `claudekit`.

Generated compatibility aliases:

- `env-setup`: environment inspection, mise setup, and managed-dotfile workflow
- `font-setup`: terminal font detection, installation, and configuration workflow
- `theme-setup`: Catppuccin flavor selection and multi-target theme sync workflow
